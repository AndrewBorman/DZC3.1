#include <stdio.h>

int k, l, m, sum;

int main()
{
    printf("Wwedite tri 4isla\n");
    scanf("%d %d %d", &k, &l, &m);
    sum = k+l+m;
    printf("%d + %d + %d = %d", k, l, m, sum);

    
    return 0;
}
